{
  "name": "vinq-crm",
  "version": "2.0.0",
  "description": "VinQ CRM - Zoho Style CRM for Real Estate",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:backend": "cd backend && npx prisma generate && npx prisma migrate deploy && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start:backend": "cd backend && npm start",
    "preview:frontend": "cd frontend && npm run preview",
    "prisma:generate": "cd backend && npx prisma generate",
    "prisma:migrate": "cd backend && npx prisma migrate deploy",
    "vercel-build": "echo '🔧 Installing backend dependencies...' && npm install --prefix backend && echo '🔨 Generating Prisma Client...' && cd backend && npx prisma generate && echo '🏗️ Building backend...' && npm run build && echo '📦 Installing frontend dependencies...' && cd ../frontend && npm install && echo '🎨 Building frontend...' && npm run build && echo '✅ Vercel build complete!'",
    "postinstall": "npm install --prefix backend && cd backend && npx prisma generate || true"
  },
  "keywords": [
    "crm",
    "real-estate",
    "zoho",
    "react",
    "typescript",
    "express",
    "mongodb"
  ],
  "author": "VinQ Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@prisma/client": "^7.0.1"
  }
}
